[project]
name = "shared"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sergeyv" }
]
requires-python = ">=3.14"
dependencies = [
    "faststream[rabbit]>=0.7.2",
    "loguru>=0.7.3",
    "opentelemetry-api>=1.44.0", # logging.py: get_current_span
    "opentelemetry-exporter-otlp-proto-grpc>=1.44.0", # tracing.py: OTLPSpanExporter
    "opentelemetry-sdk>=1.44.0", # tracing.py: TracerProvider
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
