apiVersion: 1
providers:
  - name: default
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true      # keep editing in the UI; re-export to persist
    options:
      path: /etc/grafana/provisioning/dashboards